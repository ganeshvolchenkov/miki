openai>=1.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
tzdata>=2024.1

# Optional: only needed to use the Google Calendar tool. Miki runs fine
# without these -- Calendar just reports itself as unavailable.
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
networkx>=3.0
PyYAML>=6.0
customtkinter>=5.2
streamlit>=1.30
